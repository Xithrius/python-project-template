[project]
name = "python-project-template"
version = "0.1.0"
description = "A template for Python projects."
authors = [{ name = "Xithrius", email = "xithrius@gmail.com" }]
requires-python = ">=3.13"
license = { text = "MIT" }
dependencies = []

[dependency-groups]
dev = ["ruff>=0.14.10", "ty>=0.0.10"]
